this work include three program:
1. the network_monitor.py is to monitor the network performance,include the port performance and the flow performance.
2. the network_aware.py is to monitor the network topology. when the topology change to update the topology infomation.
3. shortest_route.py is the route the flow in shortest route. moreover, it has fast recovery function from link failure.
